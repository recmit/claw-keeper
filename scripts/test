#!/bin/sh
set -eu

repo_root=$(git rev-parse --show-toplevel)
cd "$repo_root"

if ! command -v uv >/dev/null 2>&1; then
  echo "scripts/test: uv is required to run tests." >&2
  exit 1
fi

UV_CACHE_DIR="${UV_CACHE_DIR:-.uv-cache}" uv run pytest "$@"
